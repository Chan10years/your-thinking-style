# AGENTS.md

## Project

本项目是一个基于 AI 大模型辅助找出明确错误、还原用户思路并给出修正方向的代码学习平台。

用户可以粘贴算法题目、自己的 C++ 代码，并可选择填写“我的思路或卡点”和失败信息。系统通过 DeepSeek 生成结构化分析与标注，帮助用户理解错误原因、代码实际行为、原思路是否可继续修成正解，以及下一步应如何修正。

当前阶段目标是实现一个界面类似于 LeetCode 和蓝桥云课的错题分析辅助网站。产品重点是顺着用户的思路做诊断，而不是直接给出标准答案。

## Tech Stack

- 应用框架：Next.js
- 前端界面：React
- 开发语言：TypeScript
- 样式方案：Tailwind CSS
- UI 组件：shadcn/ui
- 代码编辑器：Monaco Editor
- 运行环境：Node.js

## Document Priority

- 产品定位、目标用户、核心问题、差异化价值、产品原则和长期方向以根目录 `PRODUCT.md` 为准。
- 当前版本详细规格、数量限制、接口行为、异常处理和验收标准以根目录 `MVP.md` 为准。
- DeepSeek 结构化返回 Schema 已在 `MVP.md` 第 3 节冻结，后续实现必须遵守其中的字段、枚举、结构和校验规则，未经明确要求不得自行修改。
- 如果 `AGENTS.md` 与 `PRODUCT.md` 或 `MVP.md` 冲突，优先遵循 `MVP.md` 中的当前版本规格，其次遵循 `PRODUCT.md` 中的产品原则。

## Development Guardrails

- 不改变已经确认的产品决策。
- 不擅自新增产品规则、交互规则或数据结构。
- 不擅自修改 `MVP.md` 第 3 节定义的 DeepSeek 结构化返回字段、枚举、结构或校验规则。
- MVP 只支持 C++；不要加入多语言切换。
- 不加入代码运行、编译、判题或失败样例真实性验证。
- 不加入橙色疑似错误标注。
- 红色只表示明确且可确认的错误。
- 诊断标签统一使用“思路还原”“错误解释”“修正方向”。
- 所有 MVP 细节以根目录 `MVP.md` 为唯一来源，不在本文件复述完整 MVP。

## Working Rules

- 修改前先阅读相关代码和现有结构。
- 优先复用现有组件与样式。
- 一次只处理当前任务。
- 不擅自增加功能、更换技术栈或大规模重构。
- 发现冲突或明显风险时，先说明问题，再采用影响最小的方案。
- 保持代码简单、清晰、可维护。
- 修改文档时保持三份文档职责分离：
  - `PRODUCT.md` 写产品方向。
  - `MVP.md` 写当前版本规格和验收标准。
  - `AGENTS.md` 写开发协作规则。
- 仅做文档整理时，不修改 Next.js 页面、组件、API 路由或业务代码。

## Verification

完成代码任务后必须：

- 运行 `npm run lint`，检查代码规范。
- 运行 `npm run build`，确认项目可以正常构建。
- 必要时运行 `npm run dev`，检查页面和本次修改涉及的交互。
- 验证本次修改涉及的主流程。
- 验证 API Key 不会被持久化或记录日志。
- 如果某个命令无法执行，必须说明原因。
- 总结修改文件、实现结果和验证情况。

仅修改文档时：

- 不要求运行 `npm run lint` 或 `npm run build`。
- 需要检查三份文档是否存在术语、路径、数字或范围冲突。
- 需要说明是否修改了任何代码文件。

## Safety

- 未经允许，不删除文件或重置配置。
- 未经允许，不安装新的大型依赖。
- 不修改与当前任务无关的文件。
- 不提交密钥、密码或本地环境配置。
- 不记录、打印或持久化用户 API Key。
- 公开网页中的 API Key 处理必须遵守根目录 `MVP.md` 的安全要求。
