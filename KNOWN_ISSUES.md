# KNOWN_ISSUES.md

## 当前状态

阶段 6 已完成。

- 阶段 4：DeepSeek 服务端接口接入已完成。
- 阶段 5A：分析后三栏结果页已完成。
- 阶段 5B：AI 诊断工作区轻量 Docking 已完成，并通过真实浏览器交互验收。
- 阶段 6：Monaco 红蓝代码标注已完成，并通过 exactCode 定位测试、标注映射测试和真实浏览器验收。
- 真实分析稳定性修复已验收通过，不再列为未解决问题。

---

## ISSUE-001：模型返回的行列位置可能不准确

### 现象

真实分析结果中可能出现 `exactCode` 能在原始代码中找到，但模型返回的 `startLine`、`startColumn`、`endLine`、`endColumn` 与实际代码位置不一致。

### 原因

大模型对空行、缩进、换行和重复代码片段的计数不稳定，不能完全依赖模型生成行号和列号。

### 影响

第六阶段接入 Monaco 标注时，可能出现：

- 红色标注偏移
- 蓝色标注偏移
- 点击错误编号后定位到错误行
- 同一代码片段存在多处时匹配错误

### 处理方案

第六阶段已实现程序化定位：

```text
模型返回 exactCode
→ 在用户原始代码中查找 exactCode
→ 程序计算 startLine、startColumn、endLine、endColumn
→ 使用程序计算结果进行 Monaco 标注
```

模型返回的行列位置只作为辅助信息，不能作为最终可信来源。

重复片段处理：

- `exactCode` 唯一匹配时直接使用程序计算范围。
- `exactCode` 多次匹配时，使用模型起始行列与候选真实起点的距离消歧。
- 如果多个候选距离相同，则视为无法可靠定位，不生成标注。

定位失败处理：

- `exactCode` 为空、匹配不到、重复片段无法消歧、计算范围无效时，不在 Monaco 中创建 decoration。
- 红色错误仍保留诊断文本，并显示“代码位置未能可靠定位”。
- 不使用模糊匹配、空白纠正或猜测位置。

### 验收标准

- `exactCode` 能在原代码中唯一匹配
- 程序自动计算行列位置
- 红蓝标注位置与原代码一致
- 空格和换行保持原样
- 重复片段有明确处理策略

### 状态

已解决。阶段 6 已通过自动化测试与真实浏览器验收。

---

## ISSUE-002：实际请求模型需要确认固定为 DeepSeek V4 Pro

### 现象

真实接口已经调用成功，但仅凭返回 JSON 无法证明实际调用模型为：

```text
deepseek-v4-pro
```

DeepSeek 控制台曾显示 Flash 调用记录。

### 风险

如果请求体仍使用以下模型名，可能实际调用 Flash：

```text
deepseek-chat
deepseek-reasoner
deepseek-v4-flash
```

### 检查位置

```text
src/lib/deepseek.ts
```

重点检查：

- 模型常量
- fetch 请求体中的 `model`
- 测试中的请求体断言
- 是否存在多个模型配置来源

### 目标实现

模型名称集中定义：

```ts
const DEEPSEEK_MODEL = "deepseek-v4-pro";
```

真实请求体必须使用该常量。

### 验收标准

- 代码中模型常量严格等于 `deepseek-v4-pro`
- 接口测试断言 fetch 请求体的 `model`
- 项目中不存在旧模型名或 Flash 模型名
- DeepSeek 控制台显示 Pro 请求记录

### 状态

待确认，非阶段 5 阻塞项。

---

## ISSUE-003：Monaco worker fallback 警告

### 现象

阶段 5 和阶段 6 真实浏览器验收中，Monaco 可以正常加载、显示、滚动、渲染红蓝 decoration，并能在页面尺寸变化后保留标注，但浏览器控制台仍出现 worker fallback 提示。

### 影响

当前不阻塞阶段 6：编辑器作为结果页和工作区内容载体可以正常使用，红蓝标注也能正常显示。

后续如果要提升编辑器语言服务、性能或控制台整洁度，可以单独配置 Monaco worker。

### 状态

观察项，非阶段 6 阻塞项。

---

## 已解决

### 结果页只显示固定成功提示

阶段 5A 已完成完整结果页渲染，页面不再只显示“分析完成”提示。

### AI 诊断工作区轻量 Docking 待实现

阶段 5B 已完成轻量 Docking，支持标签点击、激活状态、排序、合并、上/下/左/右分栏、splitter 调整和恢复默认布局。

### 第一次 Schema 失败后的完整重新分析稳定性问题

已完成稳定性修复并通过验收：首次 JSON 解析成功但 Schema 校验失败时最多触发一次完整重新分析；第二次仍失败时返回统一 `INVALID_MODEL_RESPONSE`。
