# PRODUCT.md

## 产品名称

YourThinkingStyle

## 产品定位

YourThinkingStyle 是一个面向算法学习者的 AI 代码分析平台。

用户提交算法题目、自己的代码，并可选择补充自己的思路或卡点、失败信息。系统通过 AI 生成结构化分析与代码标注，帮助用户理解：

* 自己原本想怎么解题。
* 代码实际实现了什么。
* 原思路是否能够继续修成正解。
* 明确错误发生在哪里。
* 下一步应该如何沿原思路修正，或为什么必须更换思路。

产品重点不是直接给标准答案，而是还原用户的思考过程，判断用户的思路与代码实现之间的关系，并找出认知断点。

当前 MVP 的详细范围、数量限制、接口行为、异常处理和验收标准以根目录 `MVP.md` 为准。

## 目标用户

* 正在学习数据结构与算法的学生。
* 能够写出部分代码，但难以定位问题的初学者。
* 能看懂标准答案，却不理解自己原有思路为什么失败的用户。
* 正在进行 LeetCode、蓝桥杯、CSP、PAT 等算法练习的用户。

## 核心问题

传统在线判题通常只能告诉用户答案是否通过，无法解释：

* 用户正在使用什么解题思路。
* 用户描述的思路和代码实际实现是否一致。
* 用户原思路是否能够继续修成正解。
* 核心算法代码位于什么位置。
* 明确错误从哪里发生。
* 不同错误分别会造成什么结果。
* 用户应该如何从原有思路继续推导正确答案。

普通 AI 对话也容易直接生成完整答案，导致用户跳过对自己错误过程的理解。

## 差异化价值

YourThinkingStyle 的核心价值是“顺着用户思路诊断”。

系统不仅分析代码是否可能出错，还要尽量还原用户的推理链条，并判断：

* 思路本身是否成立。
* 思路能否沿原方向修成正解。
* 用户描述的思路和代码实际实现是否一致。
* 错误来自算法理解、实现细节还是边界处理。
* 哪些结论有失败信息支持，哪些只是静态推测，哪些证据不足。

失败信息帮助 AI 判断“代码哪里错”。“我的思路或卡点”帮助 AI 判断“用户为什么会这样写”。后者是产品名称和核心体验的来源。

## 核心使用流程

1. 用户粘贴算法题目。
2. 用户粘贴自己的代码。
3. 用户可选择填写“我的思路或卡点”。
4. 用户可选择填写失败信息。
5. 用户填写自己的 DeepSeek API Key。
6. 用户点击“开始分析”。
7. 系统通过服务端转发调用 DeepSeek，生成结构化诊断结果。
8. 页面展示代码标注和诊断区域。
9. 用户可以修改题目、代码或补充信息并重新分析。

## 产品结构概述

分析前，页面以题目区和代码区为主，并提供默认折叠的补充信息区域。

补充信息包括：

* 我的思路或卡点。
* 失败输入。
* 预期输出。
* 实际输出或报错。

分析后，页面切换为题目、代码和诊断三栏工作区。

AI 诊断工作区默认包含三个诊断模块：

* 思路还原
* 错误解释
* 修正方向

代码区只使用三种视觉状态：

* 普通代码。
* 蓝色核心算法或疑似核心算法。
* 红色明确且可确认的错误。

MVP 不引入橙色疑似错误标注。

## 产品原则

* 优先还原用户思路，再给出修正方向。
* 优先沿用户原思路修正；只有原思路不可行时，才说明为什么必须换思路。
* 不把普通疑点、风格问题、可替代写法标成明确错误。
* 不把推测包装成确定结论。
* 不编造题目条件。
* 不使用空泛鼓励代替具体分析。
* 不为了展示能力提供过度复杂的算法。
* 参考代码不应抢走思路分析的主流程。
* 用户自带 API Key 的使用必须保持透明、临时和最小化。

## MVP 阶段原则

MVP 用于验证核心闭环：

* 用户能提交题目、代码和可选补充信息。
* 系统能返回结构化分析。
* 用户能通过红蓝标注和诊断文本理解自己的思路与错误。
* 系统能明确区分确定结论、静态推测和证据不足。

MVP 不承担完整在线评测职责，不运行代码，不编译，不判题，也不验证失败信息是否真实。

MVP 的具体输入限制、标注数量上限、代码定位策略、请求限制、自动重试和异常处理均以根目录 `MVP.md` 为唯一来源。

## 长期方向

产品成熟后可以考虑：

* 支持更多编程语言。
* 增加更完整的错题和历史记录能力。
* 支持平台统一承担模型调用费用。
* 增加付费订阅或商业化能力。
* 在不破坏“顺着用户思路诊断”的前提下扩展更多模型和工作流。

这些方向不改变当前 MVP 的范围。当前版本以根目录 `MVP.md` 为准。
