# Design QA

## 阶段 8 异常处理、测试与部署验收记录

### 验收环境

- 日期：2026-07-04
- 运行方式：`next start -p 3027`
- 浏览器：Google Chrome Headless，通过 Chrome DevTools Protocol 执行真实页面操作
- 视口：使用 Chrome 默认 headless 视口
- 分析服务：浏览器验收在页面内 mock `/api/analyze`，返回通过 Schema 的 `AnalysisResponse` 或指定错误响应；未调用真实 DeepSeek，未记录真实 API Key。
- 截图记录：
  - `artifacts/stage7-browser-acceptance.png`
  - `artifacts/stage8-browser-exceptions.png`

### 实际执行的浏览器操作

1. 打开生产 `/analyze` 页面，等待 Monaco 完成渲染。
2. 填写题目、C++ 代码、我的思路或卡点和临时 API Key。
3. 首次提交时 mock `/api/analyze` 返回 500 与明确错误文案。
4. 检查页面未进入结果页，loading 结束，开始分析按钮恢复可点击，并显示错误文案。
5. 再次提交返回成功结果，进入三栏结果工作区。
6. 使用阶段 7 验收脚本覆盖红色错误代码区到诊断区、诊断区到代码区的双向联动。
7. 修改代码后重新分析，确认新结果整体替换旧结果。
8. 将诊断 Dock 拖成右侧分栏。
9. 重新分析失败时，确认旧结果文本仍保留、重新分析按钮恢复可点击、Dock 分栏仍保留。
10. 连续点击重新分析时，确认重复提交被拦截，只发送一次请求。

### 每项操作结果

- 首次分析失败后退出 loading：通过。
- 首次分析失败后允许重新提交：通过。
- 重新分析成功后整体替换结果：通过。
- 重新分析失败后保留旧结果：通过。
- 重新分析失败后保留 Dock 当前布局：通过。
- 红色错误双向联动在重新分析后仍正常：通过。
- 重复提交拦截：通过。
- API Key 未写入 localStorage/sessionStorage，浏览器验收只使用临时 mock key：通过。

### 控制台记录

- 阶段 7 生产浏览器验收中无阻塞性错误。
- 仍出现 Monaco worker fallback 警告，属于阶段 5 和阶段 6 已记录的非阻塞观察项。

### 部署准备

- 已补充 `.env.example`，只包含项目级 DeepSeek endpoint/model，不包含真实 API Key。
- 已补充 `DEPLOYMENT.md`，说明生产环境变量、缺失配置错误响应、本地生产检查和 Vercel 部署步骤。
- 当前本机未发现 `vercel` 或 `gh` CLI，也没有可用线上部署凭据；线上部署与线上主流程验收未执行。

### 阶段 8 完成条件

阶段 8 的本地异常处理、自动化测试、生产构建和本地生产模式浏览器主流程已完成。线上部署仍需部署平台凭据和目标项目配置后执行。

## 阶段 6 真实验收记录

### 验收环境

- 日期：2026-07-03
- 运行方式：`next start -p 3017`
- 浏览器：Playwright 控制的真实浏览器页面
- 视口：1440 × 900、980 × 720
- 分析服务：在页面提交前使用稳定 mock 拦截 `/api/analyze`，返回通过 Schema 的 `AnalysisResponse`；未调用真实 DeepSeek，未记录 API Key。
- 截图记录：
  - `artifacts/stage6-browser-first-result.png`
  - `artifacts/stage6-browser-resized.png`
  - `artifacts/stage6-browser-second-result.png`

### exactCode 定位算法

- 蓝色 `blueBlocks` 和红色 `redErrors` 都以 `location.exactCode` 为主定位依据。
- 定位时对用户原始代码执行完全字符串匹配，保留空格、缩进、换行和 CRLF/LF 差异，不做 trim、模糊匹配或空白纠正。
- 唯一匹配时，根据字符串 offset 重新计算 Monaco `[start, end)` 行列范围，并忽略模型行列偏差。
- 重复匹配时枚举全部候选，用模型 `startLine/startColumn` 与候选真实起点的距离消歧；距离相同则拒绝定位。
- `exactCode` 为空、完全匹配不到、重复片段无法消歧、计算范围无效时均视为定位失败。

### 降级与重叠规则

- 定位失败的条目不生成 Monaco decoration，不会猜测或标到其他代码。
- 定位失败的红色错误仍保留在“错误解释”中，并显示“代码位置未能可靠定位”。
- 红色 decoration 的 z-index 高于蓝色；红蓝范围重叠时，红色背景、下划线和编号 badge 仍优先可见，蓝色在非重叠范围保留。
- 新分析结果会替换旧 decoration；空数组不创建 decoration。

### 实际执行的浏览器操作

1. 打开生产 `/analyze` 页面，等待 Monaco 完成渲染。
2. 使用稳定 mock 返回包含 2 个蓝色块、2 个可定位红色错误、1 个定位失败红色错误的分析结果。
3. 提交第一次分析，检查结果页用户代码 Monaco 中红蓝标注。
4. 切换到“错误解释”，检查红色编号与诊断编号一致，并检查定位失败状态。
5. 调整页面到 980 × 720，检查标注仍存在且 Monaco 宽度更新。
6. 在 Dock 中切换“修正方向”再返回“错误解释”，检查标注仍存在。
7. 返回编辑，提交第二次 mock 结果，第二次结果不包含红蓝标注。
8. 检查旧红蓝标注、旧红色编号和旧诊断文本均未残留。

### 每项操作结果

- 唯一单行红色错误正确标注：通过，显示红色编号 `1`。
- 多行蓝色代码块正确标注：通过，主循环多行范围被蓝色标出。
- 同时存在多个红蓝标注：通过，第一次结果中蓝色 decoration 和红色 decoration 同时存在。
- 红色错误编号可见并与诊断内容一致：通过，代码区显示 `1`、`2`，诊断区显示“错误 1”“错误 2”“错误 3”。
- 红蓝范围重叠时红色仍清楚：通过，循环内 `ans = 0` 位于蓝色主循环内，红色标注和编号仍可见。
- 页面 resize 后标注正常：通过，980 × 720 下红蓝标注和编号仍存在。
- 切换 Dock 标签后返回，标注仍存在：通过。
- Dock 内部分栏 resize 后标注正常：本阶段未重复执行该浏览器动作；阶段 5 已验证 Dock 分栏和 splitter resize 交互本身，阶段 6 已验证标注不随页面 resize 和 Dock 标签切换丢失。
- 定位失败时不出现错误标注：通过，“错误 3”只在诊断区显示定位失败状态，代码区没有编号 `3`。
- 新结果替换旧结果后无旧标注残留：通过，第二次结果红蓝 decoration 数量均为 0，旧错误文本消失。
- Monaco 控制台没有新增阻塞性错误：通过；仅保留 Monaco worker fallback 警告，属于既有观察项。

### 控制台记录

- 生产页面验收中错误数为 0。
- 警告为 Monaco worker fallback：`MonacoEnvironment.getWorkerUrl/getWorker` 未配置，Monaco 回退到主线程 worker 加载。
- 浏览器还报告一次密码字段不在表单内的 verbose 提示，不阻塞流程。

### 未解决问题

- Monaco worker fallback 警告仍存在，继续作为非阻塞观察项。
- 阶段 6 浏览器验收未重复执行 Dock 内部分栏 splitter resize 后的标注检查；若按最严格口径，需要在后续验收中补跑这一项。
- 阶段 7 的双向联动、点击跳转、hover 联动和自动滚动未实现。

### 阶段 6 完成条件

阶段 6 已实现并通过自动化测试、lint、build 和主要真实浏览器验收。代码区能够基于 `exactCode` 程序化定位渲染 Monaco 蓝色核心块和带编号红色错误；定位失败时安全降级，不错误标注其他代码。若按严格验收清单，仍建议补跑一次 Dock 内部分栏 splitter resize 后的标注检查；除此之外，阶段 6 可以正式标记完成。

## 阶段 5A / 5B 真实验收记录

### 验收环境

- 日期：2026-07-02
- 运行方式：`next start -p 3002`
- 浏览器：Google Chrome Headless，通过 Chrome DevTools Protocol 执行真实页面操作
- 视口：1440 × 900、980 × 720
- 分析服务：使用临时 DeepSeek mock 返回一份通过 Schema 的 `AnalysisResponse`，用于稳定复现结果页和 Docking 交互；未记录 API Key、完整题目、完整用户代码或完整模型响应。
- 截图记录：
  - `artifacts/stage5-result-default.png`
  - `artifacts/stage5-dock-before-drag.png`
  - `artifacts/stage5-dock-drag-preview.png`
  - `artifacts/stage5-dock-right-split.png`
  - `artifacts/stage5-result-resized-980.png`

### 实际执行的浏览器操作

1. 打开分析页，填写题目、C++ 代码、我的思路或卡点、失败输入、预期输出、实际输出。
2. 输入临时 API Key，提交分析。
3. 等待分析成功并进入三栏结果页。
4. 分别点击「思路还原」「错误解释」「修正方向」Dock 标签。
5. 拖动标签调整同组内排序。
6. 将标签拖到其他标签组，验证合并。
7. 将标签拖到目标区域的上、下、左、右，验证生成新分栏。
8. 拖动 splitter，验证区域尺寸变化。
9. 使用恢复默认布局。
10. 连续执行排序、分栏、合并、恢复默认布局。
11. 调整页面尺寸到 980 × 720。
12. 分别滚动不同 Dock 内容。
13. 检查参考代码折叠、红色错误空状态、补充信息摘要。
14. 检查 Monaco 是否加载、是否停留在 `Loading...`、Dock 尺寸变化后编辑器尺寸是否刷新。

### 每项操作结果

- 分析成功后可以正常进入三栏结果页：通过。
- 每个 Dock 标签均可点击：通过。
- 当前激活标签可明确识别：通过，激活标签有更强背景、边框和文字层级。
- 标签可以拖动排序：通过。
- 标签可以拖到其他标签组并合并：通过。
- 标签可以拖到上、下、左、右生成新分栏：通过。
- 拖动过程中可看到清晰拖放目标和预览：通过，目标区显示「上方分栏」「右侧分栏」「下方分栏」「左侧分栏」「合并标签组」。
- splitter 可以拖动并改变区域尺寸：通过。
- splitter 的可拖动性在视觉上可理解：通过，splitter 增加了可见边界、拖拽手柄和 resize 光标。
- 分栏之间边界清晰：通过。
- 恢复默认布局正常：通过。
- 连续多次拖动、分栏、合并后内容不丢失、不重复、不明显错位：通过。
- 页面尺寸变化后布局仍可用：通过，980 × 720 下无页面级横向溢出。
- 不同标签内容可正常滚动：通过。
- 参考代码折叠、红色错误空状态、补充信息摘要正常：通过。
- Monaco 在真实浏览器中可以加载：通过。
- Monaco 未长时间停留在 `Loading...`：通过。
- Monaco 加载失败不会阻塞结果页：当前修复后未复现阻塞；编辑器加载状态已限制在编辑器容器内。
- Dock 尺寸调整后编辑器尺寸刷新：通过。

### 发现的问题

- Monaco 默认 CDN loader 在受限网络环境中请求 `cdn.jsdelivr.net` 失败，导致编辑器长时间停留在 `Loading...`。
- Dock 拖放预览最初只有透明高亮区域，对用户来说不够明确，难以判断当前操作会合并还是分栏。
- splitter 最初视觉存在感不足，虽然可以拖动，但不够容易发现。
- Chrome 验收环境会阻止外部字体请求，页面使用本地回退字体后布局仍可用。
- Monaco worker 在浏览器控制台提示 fallback worker 警告，不影响阶段 5 的编辑器展示、滚动和尺寸刷新。

### 修复的问题

- 将 Monaco 改为动态加载本地 `monaco-editor` 包，并在渲染编辑器前完成 loader 配置，避免依赖外部 CDN。
- 增强 Dock 拖放目标区，增加方向文字、虚线边界和更明显的 hover/preview 状态。
- 增强 splitter 视觉表现，增加可见拖拽手柄、边界反馈和 resize 光标。

### 未解决问题

- Monaco worker fallback 警告仍存在，但不阻塞阶段 5 内容承载；后续可单独做 worker 配置优化。
- 第六阶段仍需处理 `exactCode` 程序化定位和 Monaco 红蓝标注，阶段 5 不实现该能力。

### 自研轻量 Docking 取舍说明

- 当前 MVP 只需要三个诊断模块在结果页内支持排序、合并、分栏、尺寸调整和恢复默认布局。
- 自研轻量 Docking 复用了现有 React 状态和组件结构，避免引入重型 Docking 依赖，也避免提前加入 localStorage 布局持久化、复杂窗口管理和跨页面工作区能力。
- 该方案能覆盖阶段 5B 的真实交互需求，但不把它设计成长期 IDE 框架。若后续产品需要多文件、多窗口、持久布局或更复杂拖放规则，再评估引入成熟 Docking 库。

### 阶段 6 继续处理的问题

- 使用 `exactCode` 在用户原始代码中做程序化定位，不能完全信任模型返回的行列号。
- 接入 Monaco 红色明确错误标注。
- 接入 Monaco 蓝色思路还原标注。
- 处理重复代码片段、空格、换行和定位失败时的降级提示。

### 阶段 5 完成条件

阶段 5A 和 5B 已通过真实浏览器验收。结果页可以完整展示结构化分析内容，轻量 Docking 满足当前 MVP 的排序、合并、分栏、尺寸调整、恢复默认布局和视觉可理解性要求。阶段 5 可以正式标记完成。
